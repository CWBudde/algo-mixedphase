# algo-mixedphase Roadmap

This repository grew out of Phase 42 of [algo-dsp](https://github.com/cwbudde/algo-dsp)
("Mixed-Phase FIR Design & Alternatives") and was split off so that the paper, the
comparison harness and the interactive lab can evolve on their own schedule, without
gating algo-dsp's v1.0.

## Goal

Build a reproducible comparison around the fixed-support alternating factorisation from
Christian-W. Budde's DAGA 2012 paper. Keep identical target samples, frequency weights, tap
budgets, and delay constraints across methods, so that every number quoted in the docs can
be regenerated with `just compare`.

## Phase 1: Design methods (Complete)

- [x] Alternating minimum-phase/linear-phase factor design in `mixedphase`
      (`DesignIterative`), plus a direct phase-interpolation baseline
      (`DesignPhaseInterpolation`), analysis metrics, tests, documentation, and a runnable
      CSV example.
- [x] Discrete-Hilbert-transform minimum-phase reconstruction alongside the real-cepstrum
      implementation; spectral-factorisation error, numerical sensitivity and runtime
      compared in `mixedphase/doc.go`.
- [x] Prescribed complex-response design with weighted least squares
      (`DesignComplexLeastSquares`), followed by a LawsVjton/IRLS path for peak-error control.
- [x] Direct magnitude-constrained, low-group-delay optimisation after Wu–Gao–Teo
      (`DesignLowGroupDelay`), with optimiser convergence and initialisation sensitivity
      documented — including the local minimum reached from a linear-phase start.
- [x] Hybrid IIR/FIR graphic EQ (`graphiceq`) as a structure-specific comparison for
      band-gain targets, kept out of the general mixed-phase API.

## Phase 2: Conditioning of the alternating correction loop (Complete)

Building the WebAssembly lab surfaced this and it blocks reproducible comparison, so it
comes before the benchmark suite.

`DesignIterative` with a nonzero delay was neither convergent nor platform-independent at
its default iteration count. On the 129-tap, 0.08-cutoff low-pass at delay 8, the RMS
magnitude error runs 5.37, 4.61, 4.92, 4.69, … 4.61 dB over passes one to six and then
_rises_ to 10.74 at the default twelve — the loop is past its best point, not converged, and
the old `ToleranceDB` early stop never fired. Worse, native and WASM builds agree to ten digits
through four passes, differ in the fourth digit at six, and reach 10.74 against 15.83 at
twelve: the truncated-factor division amplifies rounding differences geometrically. The
other three methods are bit-identical across both builds, so this is specific to the
correction loop. The loop now discards the first rising pass, which returns the reproducible
two-pass local minimum for this case.

- [x] Establish which pass count is actually best, and whether the divergence is inherent to
      the alternating scheme or an artefact of the regularised division (`Epsilon`).
- [x] Make the early stop trigger on the error _rising_, not only on it settling.
- [x] Add a cross-build determinism test (native vs `GOOS=js`) so this cannot regress silently.
- [x] Re-derive the quoted comparison numbers once the loop is stable, and state which pass
      count produced them.

## Phase 3: Common benchmark and reference suite (Complete)

- [x] Cover low-pass, parametric-EQ, crossover, deep-notch, and measured room-correction
      targets with one harness shared by all methods.
- [x] Record magnitude error, group delay, pre-ringing/energy metrics, coefficient range,
      iterations, and runtime per method and target.
- [x] Publish the results as a committed CSV plus a table in
      `docs/MIXED_PHASE_FILTER_DESIGN.md`, regenerated by a single command.

Exit criteria:

- [x] All methods share one comparison harness and publish reproducible quality/runtime
      results under fixed tap and delay budgets.
- [x] The docs state which method wins under which constraint, and where each one fails.

## Phase 4: Mixed Phase Lab comparison workbench (In progress)

The first WebAssembly lab already runs the four mixed-phase methods in the browser and
shows the realised magnitude, group delay, impulse response, and metrics. The next version
should turn that capable prototype into a comparison instrument. Its central question is:
under the same target and tap budget, what does one design buy in latency, and what does it
give up in magnitude accuracy or pre-ringing?

Visual direction: an audio measurement bench rather than a generic dashboard — warm
near-black surfaces, quiet graph-paper grids, off-white type, cyan for design A and amber
for design B. Numeric readouts should be compact and tabular; colour must always be backed
by line style, labels, or shape.

Foundation:

- [x] Drive all four Go design methods directly through WebAssembly.
- [x] Plot the realised magnitude, group delay, and impulse response rather than only the
      design grid.
- [x] Expose method-specific delay or tolerance controls and the common quality metrics.

Redesign:

- [x] Replace the single result with A/B design slots that share the target and tap budget
      but have independent method and delay/tolerance settings. Start with A pinned as the
      reference and make “swap A/B” and “copy A to B” one-action operations.
- [x] Reorganise the page into a compact experiment bar, a sticky comparison summary, and
      one analysis column. Keep the common controls visible; reveal optimiser budgets and
      method-specific parameters as advanced controls.
- [x] Overlay target, A, and B on plots with labelled axes, units, legends, linked
      crosshairs, and stable scales. Centre impulse responses on their peaks, shade the
      pre-ringing region, and add cumulative-energy curves so a lower peak is not mistaken
      for less pre-ringing.
- [x] Present metric deltas, not only raw values: RMS and peak magnitude error, effective
      delay/energy centroid, energy before peak, iterations, and runtime. Explain locally
      when a metric is unreliable, for example group delay in a deep stopband.
- [x] Add deterministic, level-matched transient audition through the Web Audio API.
      Switching A/B must preserve playback position and gain, and the unfiltered source
      must remain available as a third reference.
- [x] Add shareable experiment presets, encode the complete experiment in the URL, and
      provide exports for taps and plotted data so a browser result can be reproduced in a
      test, notebook, or bug report.
- [ ] Import the additional target presets from Phase 3 once its common benchmark fixtures
      exist.
- [x] Move design work off the UI thread, debounce live controls, and discard stale
      results. Preserve keyboard operation, visible focus, reduced-motion preferences, and
      a useful single-column layout on small screens.
- [x] Add browser smoke tests for URL restoration, A/B switching, stale-result rejection,
      and native/WASM metric agreement; deploy the tested build to GitHub Pages.

Exit criteria:

- [x] A visitor can compare two methods under one fixed target/tap budget and understand
      the magnitude-error, delay, and pre-ringing trade-off without reading the paper.
- [x] The same transient can be switched among source, A, and B without a level or timeline
      discontinuity.
- [x] A shared URL recreates the experiment deterministically, and its exported metrics
      agree with the native comparison harness within the documented cross-build tolerance.

## Phase 5: Paper (In progress)

Author the revised English paper in Typst under `docs/paper/`; the `.typ` sources, not an
edited PDF, are canonical. Follow the small build setup used by `../dig-the-diff`: `just`
recipes for build/watch, `typstyle` through treefmt, and a dedicated CI build. The tooling
is now in place, and the completed Phase 3 reference suite is the sole source for
quantitative results and conclusions.

Paper and evidence:

- [x] Create a maintainable Typst source tree with a single entry point, shared style and
      notation definitions, a BibTeX bibliography, native chart helpers, and a directory
      reserved for externally generated figures. Keep the original German DAGA paper as a
      cited historical source; only redistribute its PDF if its publication terms permit
      that.
- [x] Establish an AES-informed author-manuscript design with a full-width title and
      abstract, a two-column body, numbered sections and figures, bracketed references,
      and figure encodings that remain distinguishable in greyscale. Do not reproduce the
      protected AES publication header or imply that the working paper is an AES preprint.
- [x] Add the first data-backed chart set in Typst: accuracy versus mean delay, energy
      before the impulse-response peak, and the hybrid graphic-EQ latency/error trade-off.
      Read both committed Phase 3 CSVs directly so a normal paper build has no chart
      runtime dependency.
- [x] Produce a revised English treatment, not merely a translation. Clearly separate the
      2012 alternating-factorisation contribution from the later implementations,
      baselines, and comparative results added by this repository.
- [x] Match the paper's symbols and algorithm steps to the public Go API, and include a
      reproducibility appendix mapping each algorithm, numbered result, figure, and table
      to its implementation, test or Phase 3 CSV, configuration budget, and regeneration
      command.
- [x] Complete the plots and result tables from the committed Phase 3 data; do not
      hand-edit values into Typst. Add representative magnitude, group-delay, and
      peak-aligned impulse-response figures, then record tap counts, delay constraints,
      frequency grids, weights, tolerances, iteration limits, and the repository commit in
      the paper metadata or captions.
- [x] Give failure modes the same visibility as successful cases: correction-loop
      instability, optimiser initialisation sensitivity, zero-weight bins, unreliable
      stopband group delay, and target classes that do not fit the hybrid graphic-EQ
      structure.

Build and publication:

- [x] Add `just paper` and `just paper-watch`. A normal paper build consumes committed
      benchmark artifacts and produces `docs/paper/mixed-phase-filter-design-en.pdf`
      without rerunning timing benchmarks.
- [x] Add the data/figure refresh recipe for the committed Phase 3 CSVs.
- [x] Add Typst formatting to `treefmt.toml` with `typstyle`, and pin the Typst compiler and
      all imported Typst package versions used locally and in CI.
- [x] Add a clean-checkout CI job that formats/checks the sources, compiles the paper, and
      uploads the PDF as an artifact.
- [x] Attach the CI-built PDF to tagged releases rather than relying on a manually committed
      binary.
- [x] Link the built paper from the repository README, the Mixed Phase Lab, and the
      implementation notes. Keep those links valid when browsing a tagged release.

Exit criteria:

- [x] A clean checkout with the documented pinned toolchain builds the English PDF with
      `just paper`, with no manually prepared figures or tables.
- [x] Every quantitative claim is traceable to a test or `just compare` artifact and states
      the exact design and optimiser budget that produced it. (Re-audited: the
      low-group-delay figures, the delay sweep and the graphiceq zigzag figures were
      quoted without a test or a budget, and one named fixture did not exist. Pinned by
      `TestLowGroupDelayQuotedImprovement`, `TestIterativeDelaySweep` and
      `TestZigzagPeakErrorsBySplit`.)
- [x] Technically review the revised paper against the German original, the Go
      implementation, and the committed benchmark data.
- [ ] After the Phase 6 version tag exists, verify that its CI-built PDF is downloadable
      from the release asset URL. The workflow and durable links are already in place.

## Phase 6: Release (Planned)

- [ ] Stabilise the public API of `mixedphase` and `graphiceq`; tag v0.1.0.
- [ ] Track a supported algo-dsp version explicitly in `go.mod` and the README.
