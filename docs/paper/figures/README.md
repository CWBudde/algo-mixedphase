# External paper figures

The current benchmark charts are generated inside `charts.typ` directly from the committed
CSV artifacts, so a normal paper build does not write files here. This directory is reserved
for future response, group-delay, and impulse plots that need an external renderer.

Do not edit generated plots by hand. Each future generator must document its input CSV,
configuration budget, and regeneration command.
